{
  "name": "superblocks-auth0-embedded-app",
  "version": "1.0.0",
  "description": "Superblocks embedded app with Auth0 login and Post-Login Action SSO.",
  "scripts": {
    "start": "npm run dev --prefix ./app",
    "dev:app": "npm run dev --prefix ./app",
    "build:app": "npm run build --prefix ./app",
    "build": "npm run build --prefix ./app"
  },
  "author": "Kris Harbold",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
